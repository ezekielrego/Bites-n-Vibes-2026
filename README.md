# Bites & Vibes

This project now runs as an Expo React Native app with a lighter, more compact UI treatment than the original web mockup.

## Run Locally

1. Install dependencies with `npm install`
2. Start Expo with `npm run start`
3. Open it in Expo Go, an emulator, or run `npm run web` for a browser preview
